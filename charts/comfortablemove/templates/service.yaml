apiVersion: v1
kind: Service
metadata:
  name: {{ include "comfortablemove.fullname" . }}
  labels:
    {{- include "comfortablemove.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "comfortablemove.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
